<?xml version="1.0" encoding="UTF-8"?>
<!-- This XML file allows us to easily customize the visual aesthetics of the user interface. Note that this properties
file is only for the standard high-level controls for an application, so it does not include properties meant for any
functionality within the main workspace. -->
<properties>
    <property_list>
        <property name="LOAD_ERROR_MSG_FORMAT" value="%s because %s"/>
        <property name="SAVE_ERROR_MSG_FORMAT" value="%s because %s"/>

        <property name="ALGORITHM_INTERRUPTED_TITLE" value="Algorithm was interrupted!"/>
        <property name="ALGORITHM_INTERRUPTED_MESSAGE" value="Algorithm was interrupted while it was running..."/>
    </property_list>
    <property_options_list/>
</properties>
